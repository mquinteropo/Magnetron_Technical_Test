# Base de datos Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=billing_db
POSTGRES_USER=app
POSTGRES_PASSWORD=app_pwd

# JWT configuración (requerido por Program.cs)
JWT_SECRET=K3u
JWT_ISSUER=magnetron.billing.api
JWT_AUDIENCE=magnetron.billing.clients
JWT_EXP_MINUTES=60

# Nota: No dejar valores sensibles en repositorio público. Usar variables de entorno en despliegue.
